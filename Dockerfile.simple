# Ultra Simple LeadHub Dockerfile for debugging
FROM node:18

WORKDIR /app

# Copy everything first
COPY . .

# Install with npm install (not ci)
RUN npm install --legacy-peer-deps

# Build
RUN npm run build

# Environment
ENV NODE_ENV=production
ENV PORT=80

EXPOSE 80

# Start
CMD ["npm", "start"]
